name: ${project.artifactId}
main: com.heroslender.magnata.${project.artifactId}
version: ${project.version}
author: Heroslender
depend: [Vault]
softdepend: [Legendchat, HolographicDisplays, Citizens, PlaceholderAPI, NameTagEdit]

commands:
  magnata:
     description: Ver o magnata atual.
     usage: §c/<command>§7 - §eVer o magnata atual